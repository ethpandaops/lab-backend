# Frontend Configuration
# Leave empty to use latest stable release (default)
# Priority: FRONTEND_SOURCE > FRONTEND_BRANCH > latest stable release

# FRONTEND_SOURCE - Copy from local frontend dist/ directory (for development)
FRONTEND_SOURCE=../lab

# FRONTEND_BRANCH - Download latest release matching branch name (branch → branch-v*)
# Examples:
#   FRONTEND_BRANCH=feature-xyz    (matches feature-xyz-v0.0.x releases)
# Leave empty or comment out to use latest stable release:
# FRONTEND_BRANCH=
